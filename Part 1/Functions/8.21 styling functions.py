"""
functions should have descriptive name, lowercase and _
every function should have a comment that explains concisely what the function does
a default value for a parameter, no space should be used on either side of the equal sign

"""
def function_name(parameter_0, parameter_1='default value'):

#the same convention should be used for keyword arguments in function calls
function_name(value_0, parameter_1='value')

def function_name(
        parameter_0, parameter_1, parameter_2,
        parameter_3, parameter_4, parameter_5
):

import module_name
from module_name import function_name
from module_name import function_name as fn
import module_name as mn
from module_name import *
